!
!
! In order for this mapping to work, edit xorg.conf and set your
! keyboard to "us(intl)", then reboot.
!
! This file must be saved to ~/.Xmodmap
! To revert to regular keyboard behavior, run the command "setxkbmap".
! To re-execute the mapping in this file, run "xmodmap ~/.Xmodmap"
!
!

! Map Caplock as Control and Control as Escape
remove Lock = Caps_Lock
remove Control = Control_L
keycode 66 = Control_L
keycode 37 = Escape
add Control = Control_L
